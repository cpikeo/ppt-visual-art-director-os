# Runtime dependencies for ppt-visual-art-director-os
# Python 3.10+
python-pptx>=1.0.2,<2.0   # 原生可编辑 PPTX 生成
Pillow>=10.0,<13.0        # ghost 方向预览（PIL 即渲染器）
numpy>=1.24               # 资产 QC 的像素统计
PyYAML>=6.0               # brief（yml）解析
# 不需要任何系统级组件：不含外部渲染器链路，也不启动 office 套件。
